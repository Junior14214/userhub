# userhub
UserHub is a RESTful API built with Spring Boot for managing users and their external projects. Features Basic Auth, full CRUD, Flyway migrations, Docker &amp; Docker Compose setup, structured JSON logs and Prometheus metrics. Java 21 | Spring Boot | MySQL | Docker
